<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nkle.poet">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Poet">
        <activity android:name=".UserProfile"/>
        <activity android:name=".AddPost" />
        <activity android:name=".PostDetail" />
        <activity android:name=".Profile" />
        <activity android:name=".Register" />
        <activity android:name=".Login" /> <!-- <activity android:name=".Slider" /> &lt;!&ndash; <activity android:name=".Home" /> &ndash;&gt; -->
        <activity android:name=".Slider" />
        <activity android:name=".MainActivity" /> <!-- <activity android:name=".Register" /> -->
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>